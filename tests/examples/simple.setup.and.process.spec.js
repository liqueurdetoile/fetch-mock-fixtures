import {Server} from '@';

const server = new Server();

describe('Simple setup and process', function() {
  afterEach(() => server.reset(true));

  describe('Error management', function() {

  })
})
